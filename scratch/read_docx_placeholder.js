import fs from 'fs';
import path from 'path';
import admZip from 'adm-zip'; // Wait, do we have adm-zip? We might not have it in package.json.
// Let's write a PowerShell script instead or use Node.js built-in 'zlib' / 'unzip' or just use PowerShell's Expand-Archive!
// Yes, PowerShell has built-in support for zip files and XML parsing.
